import React from "react";
import ReactDOM from "react-dom/client";
import FastWorkApp from "./FastWorkApp";
import "../styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FastWorkApp />
  </React.StrictMode>,
);
