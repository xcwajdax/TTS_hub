# Fast Work bundle (release)

Ten folder jest wypełniany skryptem `scripts/build-fast-work-bundle.ps1` przed bundlowaniem głównej aplikacji TTS Hub.

Zawartość po buildzie:

- `TTS Hub Fast Work.exe` (+ pliki runtime Tauri/WebView2 z katalogu release)

Pliki binarne nie są commitowane — tylko ten README.
