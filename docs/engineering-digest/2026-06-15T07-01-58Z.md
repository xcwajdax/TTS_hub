# Daily Engineering Digest

**Period:** 2026-06-14 07:00 UTC → 2026-06-15 07:00 UTC  
**Repository:** [xcwajdax/TTS_hub](https://github.com/xcwajdax/TTS_hub)  
**Generated:** 2026-06-15T07:01:58Z (automation cron)

---

## At a glance

**High activity on `main` (docs) and `feat/voicebox-backend-fork` (major feature work).** No PRs merged in the window; **[PR #6](https://github.com/xcwajdax/TTS_hub/pull/6)** is the dominant in-flight change (+29,763 / −477 across 368 files). New **Voicebox backend** CI workflow is **failing** on Linux.

| Metric | `main` | `feat/voicebox-backend-fork` |
|--------|--------|------------------------------|
| Commits (24h) | 3 | 12 |
| Merged PRs | 0 | — (open) |
| Primary theme | README / promo docs | Voicebox fork + UI + CI |
| CI | — | Voicebox backend: **4/4 failed** |

---

## Themes

### 1. README & publication polish (`main`)

Three commits landed on `main`, all documentation-facing:

| Commit | Author | Summary |
|--------|--------|---------|
| [`06b5374`](https://github.com/xcwajdax/TTS_hub/commit/06b5374) | Manny (cron) | Hero screenshot refresh, embedded demo video, roadmap section, new [`docs/VOICEBOX_FORK.md`](docs/VOICEBOX_FORK.md) (+89 lines) |
| [`bc58de0`](https://github.com/xcwajdax/TTS_hub/commit/bc58de0) | Manny (cron) | Shorter Maklowicz narration clip (~24s, 506 KB vs 3.9 MB); absolute GitHub raw URL for README video player |
| [`62f373b`](https://github.com/xcwajdax/TTS_hub/commit/62f373b) | TOPKEK | Switched video `src` to relative path; caption updated to MiniMax; roadmap links now point to `voicebox-backend/` in-repo |

**User impact:** GitHub landing page is materially richer — screenshot, narrated demo, and a public Voicebox fork policy doc. No runtime or API behavior changed on `main`.

**Risk:** README roadmap and Voicebox section now reference [`voicebox-backend/`](voicebox-backend/), but that directory **does not exist on `main`** yet (only on `feat/voicebox-backend-fork`). Links will 404 until PR #6 merges.

---

### 2. Voicebox backend fork — open PR #6

**[PR #6 — feat(voicebox): fork backend v0.4.1 + upstream transparency](https://github.com/xcwajdax/TTS_hub/pull/6)** (opened 2026-06-14, updated 2026-06-15 02:21 UTC, **open**)

Largest change set in repo history for this period. Clustered into sub-themes:

#### Backend import (MIT fork)

- Imports `jamiepine/voicebox` **`backend/` at v0.4.1** into `voicebox-backend/` (~25k+ lines Python)
- Upstream transparency: [voicebox#749](https://github.com/jamiepine/voicebox/issues/749), [voicebox#750](https://github.com/jamiepine/voicebox/pull/750)
- Policy docs: `docs/VOICEBOX_UPSTREAM_*.md`, `docs/VOICEBOX_FORK_PR-body.md`
- PR body notes intentional **24h gate** before merge (upstream heads-up)

#### TTS Hub ↔ Voicebox integration (Rust)

- Client identification: `User-Agent: TTS-Hub/…`, `X-Voicebox-Client-Id: tts-hub` in [`src-tauri/src/voicebox.rs`](src-tauri/src/voicebox.rs)
- New `voicebox_server` module: status probe, dev sidecar spawn/stop when `voicebox_server_mode` is bundled
- Dev launcher: `scripts/dev/start-voicebox-backend.ps1`, contract test `scripts/test-voicebox-contract.ps1`

#### Voicebox UI & product surface (React + Rust)

Commit [`f080376`](https://github.com/xcwajdax/TTS_hub/commit/f080376) adds substantial app surface:

- **`VoiceboxView`** — multi-tab editor, sidecar process management
- **MP4 export** — `src-tauri/src/video_export.rs`, `minimax_subtitles.rs` (MiniMax karaoke MP4)
- **Voice packs** — catalog (`docs/voice-packs/catalog.json`), 5 starter manifests, build script
- **Provider avatars**, expanded `MainPanel`, HTTP API extensions in `http_api.rs`
- Build pipeline hooks: `setup:voicebox-sidecar`, `build:voicebox-server` in `package.json`

**User impact (when merged):** Local Voice Box could run from a **bundled sidecar** instead of requiring a separate Voicebox install; new editor tab, voice pack catalog, and MP4 karaoke export.

**Explicitly not in PR:** PyInstaller sidecar in the installer (Faza 4 per PR body).

---

### 3. CI — new Voicebox backend workflow (failing)

New workflow [`.github/workflows/voicebox-backend.yml`](.github/workflows/voicebox-backend.yml) on PR branch:

| Job | Status | Notes |
|-----|--------|-------|
| `python-check` | Unknown / likely pass | `compileall voicebox-backend/backend` |
| `rust-voicebox-server` | **Failed** (4 runs) | Tauri build on Ubuntu |

Failure evolution (evidence from [run 27520235481](https://github.com/xcwajdax/TTS_hub/actions/runs/27520235481)):

1. **First runs (2026-06-14):** `pkg-config` could not find `glib-2.0` — missing GTK/WebKit system libs
2. **Fix [`9d3e7b1`](https://github.com/xcwajdax/TTS_hub/commit/9d3e7b1) (2026-06-15 02:20 UTC):** Added `apt-get install` for Tauri Linux deps — **glib issue resolved**
3. **Remaining failure:** Tauri build script error: `resource path binaries/voicebox-server-x86_64-unknown-linux-gnu doesn't exist`

The `rust-voicebox-server` job runs `cargo test voicebox_server` but Tauri's bundled-resource check expects a pre-built sidecar binary that CI does not produce yet.

---

## Major merged PRs

_None in this window._

Most recent merge remains **[#5 — Feat/chat window](https://github.com/xcwajdax/TTS_hub/pull/5)** (2026-06-13, before this period).

---

## Bug fixes, incidents, risky areas

| Area | Detail |
|------|--------|
| **CI regression (PR branch)** | Voicebox backend workflow red on all 4 runs; sidecar binary missing in CI |
| **README / main mismatch** | `main` README links to `voicebox-backend/` — directory only on feature branch |
| **Large binary additions (PR branch)** | Promo assets: C4D icon renders (~60 PNG frames), HDR, audio samples, `.tmp/*.json` — review repo size / gitignore before merge |
| **Sidecar lifecycle** | Dev auto-spawn of Python backend on debug builds — verify clean shutdown and port conflicts (`:17493`) |
| **Upstream fork policy** | 24h gate from voicebox#749 not yet elapsed at digest time (~20h since issue); merge timing constraint documented in PR |

No open GitHub issues filed in this period.

---

## Security & dependencies

| Change | Location | Notes |
|--------|----------|-------|
| MIT third-party backend | `voicebox-backend/` | Full Python FastAPI stack imported; upstream MIT, pinned v0.4.1 |
| New Python deps | `voicebox-backend/requirements.txt` | Torch/transformers ecosystem — review supply chain before release |
| npm script changes | `package.json` (PR branch) | `tauri` now runs `setup:voicebox-sidecar` pre-hook |
| No npm/Cargo version bumps | `main` | Dependency lock unchanged on default branch in window |
| GitHub Actions deprecation warning | Voicebox workflow runs | `actions/checkout@v4` on Node 20 — forced Node 24 default June 16, 2026 |

Client headers (`X-Voicebox-Client-Id`) improve observability for upstream; no auth model change.

---

## Follow-ups worth attention

1. **Unblock CI before merge** — Either build `voicebox-server` binary in the workflow, stub the Tauri resource for test-only builds, or split `voicebox_server` unit tests from full Tauri compile.
2. **README link integrity on `main`** — Revert or qualify `voicebox-backend/` links until PR #6 lands, or merge the fork promptly after the 24h gate.
3. **PR #6 test plan unchecked** — Manual steps in PR body still open: dev backend start, contract test, Voice Box tab connectivity.
4. **Binary / asset hygiene** — PR adds `.tmp/` JSON, large C4D render frames, promo audio; confirm intentional tracking vs. gitignore before merge.
5. **Bundled sidecar (Faza 4)** — Installer packaging not in scope; rollout risk remains for users expecting one-click Voice Box without manual Python setup.
6. **Open PR #4 stale** — [docs: document chat, origin, and usage API surfaces](https://github.com/xcwajdax/TTS_hub/pull/4) (2026-06-11, no updates in window) — may need rebase or close after #6.

---

## Evidence

```text
# main commits (2026-06-14T07:00:00Z → 2026-06-15T07:01:00Z)
62f373b 2026-06-14 13:58:07 +0200 TOPKEK — Update video source and demo details in README
bc58de0 2026-06-14 13:57:38 +0200 Manny — docs: replace README demo video with Maklowicz short clip
06b5374 2026-06-14 13:51:23 +0200 Manny — docs: README roadmap, demo video and refreshed screenshot on main

# feat/voicebox-backend-fork commits (same window)
9d3e7b1 2026-06-15 04:20:02 +0200 — ci: install Tauri Linux deps for rust-voicebox-server job
f080376 2026-06-14 13:59:44 +0200 — feat(voicebox): Voicebox UI, editor tabs, MP4 export and voice packs
d2db626 2026-06-14 13:33:56 +0200 — feat(voicebox): import backend v0.4.1 fork and upstream docs
(+ 9 merge/chore commits)

# Merged PRs in window
(none)

# Open PRs
#6 feat/voicebox-backend-fork — +29763/−477, 368 files
#4 cursor/engineering-documentation-updates-caf5 — stale

# CI (Voicebox backend workflow)
27520235481 — failure — resource path binaries/voicebox-server-x86_64-unknown-linux-gnu doesn't exist

# Automation trigger
cron: 0 7 * * 1 (Mondays 07:00 UTC)
triggeredAt: 2026-06-15T07:00:50.673Z
```
