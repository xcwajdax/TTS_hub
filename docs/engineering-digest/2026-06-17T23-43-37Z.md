# Daily Engineering Digest

**Period:** 2026-06-16 23:43 UTC → 2026-06-17 23:43 UTC  
**Repository:** [xcwajdax/TTS_hub](https://github.com/xcwajdax/TTS_hub)  
**Generated:** 2026-06-17T23:43:37Z (automation cron)

---

## At a glance

Three commits landed on `main` in the last 24 hours — all focused on **global search / discoverability** plus a small **CI fix** for Voicebox sidecar builds. No dependency lockfile changes. One GitHub PR closure was recorded in this window without new merge-commit delta on `main`.

| Metric | Value |
|--------|-------|
| Commits on `main` | 3 |
| Merged PRs (GitHub `mergedAt`) | 1 ([#6](https://github.com/xcwajdax/TTS_hub/pull/6)) — see note below |
| Files changed (24h delta) | 16 |
| Insertions / deletions | +1,557 / −31 |

---

## Major changes on `main`

### Global search (command palette)

**Commits:** `b87e5ec`, `5ab68ca` · **Author:** Manny (cron)

**User / system impact**

- **Ctrl+K command palette** — Title bar and Plik → Edycja menu open a modal search across TTS history (session, archive, Cursor feed, bots), editor draft tabs, and exported audio/MP4 files (`GlobalSearchModal.tsx`, `TitleBar.tsx`, `App.tsx`).
- **Inline Historia filtering** — `HistoryBrowseView` gains a text filter using the same token-matching logic as the palette.
- **Quick history panel filter** — Sidebar recent-generations list (`HistoryQuickPanel.tsx`) filters in place; header button jumps to the global palette (Ctrl+K).
- **Navigation from results** — Selecting a result can jump to Historia scope, open editor drafts, play/select generations, or reveal export files in the explorer.

Core matching lives in `src/lib/globalSearch/match.ts` (multi-token scoring over titles, body text, voice/model metadata, filenames). Scopes cap at 50 results each (80 for “all”).

### CI: Linux Voicebox sidecar stub

**Commit:** `b0341bf` · **Author:** Manny (cron)

Tauri’s build script expects `src-tauri/binaries/voicebox-server-*` to exist. The `rust-voicebox-server` job in `.github/workflows/voicebox-backend.yml` now writes a no-op shell stub before `cargo test voicebox_server`, so Linux CI can compile without the PyInstaller sidecar bundle.

---

## Merged PR (GitHub record)

### [#6 — feat(voicebox): fork backend v0.4.1 + upstream transparency](https://github.com/xcwajdax/TTS_hub/pull/6)

**GitHub merged:** 2026-06-17 01:34 UTC · **Merge commit:** `fafa78f` (committer date 2026-06-16 10:54 UTC)

GitHub records this PR as merged inside the digest window, but the merge commit predates it by ~15 hours. The substantive Voicebox fork (~368 files, +29k lines) was already on `main` before this period. **No additional Voicebox feature delta** landed in the last 24 hours beyond the CI stub above.

Prior impact (for context): MIT fork of `jamiepine/voicebox` backend v0.4.1, TTS Hub client headers (`User-Agent`, `X-Voicebox-Client-Id: tts-hub`), `voicebox_server` status/spawn module, Voicebox UI tab, voice-pack catalog, and `voicebox-backend.yml` workflow.

---

## Themes

### 1. Discoverability UX

Search is entirely frontend-side: it reads in-memory generation lists, `localStorage` editor tabs, and async `listVideoExports()`. No new Tauri commands or HTTP routes. **Risk:** large archives may feel sluggish when opening the palette (video export list loads up to 500 records on each open).

### 2. Shared search primitives

`filterGenerationsByTextQuery` and `generationMatchesQuery` from `match.ts` are reused in Historia browse, quick panel, and the modal — consistent behavior, single place to fix ranking bugs.

### 3. CI hardening (Voicebox)

Follows earlier fix `9d3e7b1` (GTK/WebKit apt packages). The stub closes the remaining gap where Tauri `externalBin` paths must exist at compile time.

---

## Dependencies & security

| Change | Location | Notes |
|--------|----------|-------|
| `vitest.config.ts` added | repo root | Config only — **`vitest` not added to `package.json`** |
| `match.test.ts` (+165 lines) | `src/lib/globalSearch/` | Unit tests exist but no npm script / devDependency to run them |
| No `Cargo.toml` / lock changes | — | Rust deps unchanged in this window |
| No npm lock changes | — | Frontend deps unchanged |

No auth, secret, or API-surface changes in this window.

---

## Follow-ups worth attention

1. **Wire up Vitest** — `vitest.config.ts` and `match.test.ts` were added without `vitest` in devDependencies or a `"test"` npm script; CI likely does not run these tests yet.
2. **Manual smoke tests** — Ctrl+K toggle, scope chips, keyboard navigation in the modal, quick-panel filter vs palette parity, “reveal in explorer” on MP4/audio hits, empty-query recent items.
3. **Performance on large libraries** — `listVideoExports(500, 0)` on every palette open; consider caching or lazy scope loading.
4. **Voicebox sidecar stub** — CI stub exits 0 immediately; it validates compile/link paths only, not runtime Voicebox API behavior (contract tests remain separate / manual per PR #6 test plan).
5. **PR #6 rollout** — Bundled PyInstaller sidecar (called out as “Faza 4” / not in PR) still pending; production installs depend on external or dev-spawned backend.

---

## Evidence

```text
b87e5ec feat: add global search for history, drafts, and media files
  14 files changed, 1425 insertions(+), 19 deletions(-)

5ab68ca feat: add quick history panel text filter and Ctrl+K shortcut
  2 files changed, 126 insertions(+), 12 deletions(-)

b0341bf ci: stub voicebox-server sidecar for Linux cargo tests
  1 file changed, 6 insertions(+)

GitHub PR #6 mergedAt: 2026-06-17T01:34:30Z (merge commit fafa78f)
```

Key new modules: `src/lib/globalSearch/match.ts`, `src/components/globalSearch/GlobalSearchModal.tsx`, `src/lib/globalSearch/match.test.ts`.
